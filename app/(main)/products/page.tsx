import ProductGrid from "@/app/(main)/Components/ProductGrid"

// { products }: { products: Array<ProductProp> }
const ProductPage = async () => {
  return (<><ProductGrid category="all" /></>)
}
export default ProductPage;
